# Custom Superset configuration (optional).
# You can tune default row limits, feature flags, etc.
# This file is mounted into the Superset container via docker-compose.yml
# if you uncomment the volume line for superset_config.py.

ROW_LIMIT = 5000
